{
  "name": "secure-cloud-storage",
  "version": "1.0.0",
  "description": "End-to-end encrypted cloud storage",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "postinstall": "node -e \"require('./server/db').initializeDatabase().catch(() => {})\"",
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^7.1.5",
    "pg": "^8.11.3",
    "aws-sdk": "^2.1504.0",
    "argon2": "^0.31.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": "18.x",
    "npm": ">=9.0.0"
  }
}
